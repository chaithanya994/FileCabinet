.wrapper_with_padding
  #doc_show
    %h1.title= @doc.title
    %p= simple_format(@doc.content)

    .buttons
      = link_to "edit doc", edit_doc_path(@doc), class: "button"
      = link_to "delete doc", doc_path(@doc), method: :delete, data: {confirm: "YOU SURE???"}, class: "button"